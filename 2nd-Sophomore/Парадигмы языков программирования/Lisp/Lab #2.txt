Вариант 17

Task 1

Написать неименованные функции для задания 1 первой лабораторной работы.

((lambda (a b c)
(
   list (cdr a) (cdr b) (cdr c)
))
'(v b n j h) '((y u i) (h j k) (8) 78) '(df fg hj k l (o 0 9)))

Task 2

Написать две именованные функции, одну по заданию 2 первой лабораторной работы,
задания для второй в таблице 1.

1) (defun myFunc(a b c) (list (third a) (fourth b) (sixth c)))

(myfunc '(v b n j h) '((y u i) (h j k) (8) 78) '(df fg hj k l (o 0 9)))

2)
(defun func(list1 a b) 

   (cond 
     (
       (and (and (numberp a) (numberp b)) (> (+ a b) 100))
       list1
     )
     (T (LIST A B))
   )
)

Task 3

Определите функции (NULL x), (CADDR x) и (LIST x1 x2 x3) с помощью базовых
функций. Используйте имена NULL1, CADDR1 и LIST1, чтобы не переопределять
одноименные встроенные функции системы.

(defun null1 (x) (Null x))
(DEFUN CADDR1(X) (CADDR X))
(DEFUN LIST1(X1 X2 X3) (LIST X1 X2 X3))

Task 4

Напишите функции, которые осуществляют обращение списка из 2, 3, 4 элементов.

(defun func1(mylist) (list (cadr mylist) (car mylist)))

(defun func2(mylist) (list (third mylist) (second mylist) (first mylist)))

(defun func3(mylist) (list (fourth mylist) (third mylist) (second mylist) (first mylist)))

Task 5

Постройте функции, которые проверяют, является ли ее аргумент:
a) списком из 2, 3, 4 элементов;
b) списком из 2, 3, 4 атомов.

(DEFUN DETLIST(LIST) 
      (COND ((AND (FIRST LIST) (SECOND LIST) (THIRD LIST) (FOURTH LIST)) 'QUADRO)
            ((AND (FIRST LIST) (SECOND LIST) (THIRD LIST)) 'TRIPLE) 
            ((AND (FIRST LIST) (SECOND LIST)) 'DOUBLE)
      )
)

Task 6

Напишите функцию, которая вводит фразу на естественном языке и преобразует
ее в список. Например: «я пошел гулять» -> (я пошел гулять)

(LIST (SETQ INPUT (READ)))

Task 7

Напишите функцию, которая спрашивает у пользователя ФИО студента из группы
(список группы составлен в лабораторной работе №1) и выдает следующие данные 
о нем:
a) год рождения;
b) средний бал;
c) родителей;
d) списки свойств, присвоенные ему раньше.    

(setq III (cons (list '(09.09.1996) '(Krasnoyarsk) '((5) (5) (4.8))) (list '(Иванова Мария Ивановна) '(24.11.1962) '(Красноярский рабочий) '(Яндекс))))

(setq PPP (cons (list '(23.05.1886) '(Железногорск) '((4) (5) (5))) (list '(Петрова Мария Петровна) '(08.10.1975) '(ул. Крайняя) '(МБОУ Гимназия №3))))

(setq SWZ (cons (list '(15.12.1996) '(Енисейск) '((5) (5) (5))) (list '(Сидорова Владлена Захаровна) '(23.01.1970) '(ул. 9 Мая) '(Google))))

(case (setq input (read))
	('III III)
	('PPP PPP)
	('SWZ SWZ)
)

Task 8

Напишите функцию:
a) от одного аргумента (ФИО), замещающую в списке с данными о нем 
   подсписки со средними балами на списки свойств;
b) вычисляющую средние балы, беря данные из списков свойств.

(setq III (list '09.09.1996 'Krasnoyarsk '(5 5 4.8)))
(defun func(x)
	(setf (get x 'Labs) 5)
	(setf (get x 'Practice) 5)
	(setf (get x 'Lectures) 4)
	(rplaca (last (eval x)) (symbol-plist x))
)